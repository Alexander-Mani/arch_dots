general {
  hide_cursor = true
  grace = 0
}

background {
  monitor =
  path = /home/lexman/images/hyprpapers/falcon_minimal.jpg
  position = 200, 400
  # blur_passes = 2
  # blur_size = 2
  # noise = 0.02
  # contrast = 0.8
  # brightness = 0.75
}

input-field {
  monitor =
  size = 400, 50
  outline_thickness = 3
  dots_size = 0.33
  dots_spacing = 0.15
  dots_center = true
  dots_rounding = -1
  outer_color = rgb(24, 25, 38)
  inner_color = rgb(91, 96, 120)
  font_color = rgb(202, 211, 245)
  fade_on_empty = true
  fade_timeout = 1000
  # placeholder_text = <i>Password…</i>
  hide_input = false
  rounding = -1
  check_color = rgb(204, 136, 34)
  fail_color = rgb(204, 34, 34)
  # fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
  fail_transition = 300
  position = 0, 0
  halign = center
  valign = center
}

label {
  monitor =
  text = cmd[update:1000] date +"%H:%M"
  color = rgba(200, 200, 200, 1.0)
  font_size = 32
  font_family = JetBrains Mono
  position = 0, 210
  halign = center
  valign = center
}

